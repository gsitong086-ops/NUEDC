# 淘晶驰串口屏波形显示调试过程总结

## 一、项目目标

将 AD9226 并行 ADC 采集的波形从 VOFA+ 上位机迁移到淘晶驰 TJC8048X270_011C 串口屏上显示，实现示波器式波形查看功能。

## 二、关键技术问题及解决

### 1. 通信协议适配

- **问题**：原 VOFA+ 使用 FireWater 二进制协议（浮点+帧尾），淘晶驰使用 ASCII 文本指令+`FF FF FF` 结束符
- **解决**：新增 `hmi.c/hmi.h` 通信层，实现 `HMI_SendRaw()`、`HMI_SetText()`、`HMI_AddWave()`、`HMI_ClearWave()` 等函数，将数据封装为淘晶驰协议格式
- **关键发现**：淘晶驰 `add` 命令**只认数字控件 ID 不认 objname**，`t1_wave` 的数字 ID 为 `8`，须使用 `add 8,0,val` 而非 `add t1_wave,0,val`

### 2. 按钮干扰问题

- **问题**：新增 X+/X-/Y+/Y- 缩放按钮后，开始/暂停/清除按钮失灵
- **原因**：淘晶驰按钮按下时同时发送触摸帧（0x65 开头的 7 字节包）和 printh 字符串，触摸帧的控件 ID 与功能按钮冲突；且在 UART 中断中调用 `HMI_SendRaw` 发送数据与主循环抢占总线
- **解决**：
  - 触摸帧只做静默丢弃，统一用 printh 字符串处理所有按钮
  - 中断中只修改变量+设标志位 `zoom_dirty`，主循环安全发送缩放指令

### 3. 波形显示缩放

- **问题**：10kHz~500kHz 宽频率范围内，高频波形密集不可辨，低频幅度小
- **尝试方案**：
  - 软件 RMS 幅值自动缩放 → 幅度不稳定，帧间跳变
  - EMA 平滑缩放 → 范围越缩越小，波形变形
  - 峰值保持 → 只扩大不缩小，但初始帧峰值不准
- **最终方案**：放弃自动缩放，改用软件手动缩放：X+/X- 用指数步长控制发送点数（`step = 1 << g_hzoom`），Y+/Y- 用线性增益控制幅度（`gain = g_scale * 6`），用户手动调节

### 4. AD9226 输出极性

- **问题**：显示波形与预期反相
- **原因**：AD9226 输出为负逻辑（`CodeToVoltage` 公式中乘 `-5.0f`）
- **解决**：将幅度计算公式改为 `amp = (2048 - raw) * gain + 2048`

### 5. 帧间衔接尖峰

- **问题**：每帧波形衔接处出现尖峰/跳变
- **尝试方案**：
  - 4/8 抽头 FIR 环形滤波 → 畸变波形
  - tail 桥接（上帧末尾点补发）→ 效果有限
  - DMA 环形连续模式（不停机）→ 无改善
  - 相邻点限幅器 → 无改善
- **分析**：尖峰可能来源于 50mV 小信号在 5V 量程 ADC 上的低信噪比，属硬件层面问题

### 6. 采样率优化

- 初始：TIM8 ARR=83 → 2MHz
- 最终：TIM8 ARR=20 → 8MHz（提升 4 倍，500kHz 信号从 4 点/周期提升到 16 点/周期）

## 三、最终代码架构

```
初始化
├── AD9226 GPIO 配置（GPIOD PD0-PD11 并行读取）
├── TIM8 PWM 输出（ADC 时钟，8MHz）
├── DMA2_Stream1 环形模式（连续采集到 adc_buffer[1024]）
├── USART1 921600bps（与串口屏通信）
└── 启动 HMI（设置文本、缩放参数）

主循环
├── 等待 DMA 半满中断（HT/TC）
├── 快照半缓冲区 → snap[512]
├── 按 g_hzoom 指数步长采样
├── 按 g_scale×6 增益计算幅度
├── 封装 add 指令发送到串口屏
└── 循环

按钮中断
├── START → 清屏+开始
├── PAUSE → 暂停
├── CLEAR → 清屏+暂停
├── X+(ID 13) → g_hzoom--（水平放大）
├── X-(ID 14) → g_hzoom++（水平缩小）
├── Y+(ID 15) → g_scale++（垂直放大）
└── Y-(ID 16) → g_scale--（垂直缩小）
```

## 四、经验教训

1. 淘晶驰 `add` 命令必须用控件数字 ID，不能只靠 objname
2. 中断中避免调用阻塞式 UART 发送函数，防止数据冲突
3. 信号幅值自动缩放在低信噪比环境下难以稳定，手动控制更可靠
4. DMA 环形模式可实现连续采集无断层，但帧间衔接尖峰可能来自硬件噪声
5. 采样率越高，高频信号波形越清晰，但需权衡 GPIO+DMA 的时序极限
